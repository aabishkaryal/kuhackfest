import { CreateListingInput, Listing } from "@/types/Listings";

export async function createListing(listing: CreateListingInput) {}

export async function fetchListing(id: string): Promise<Listing> {
  return {} as Listing;
}
